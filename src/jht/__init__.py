"""jht — JAX-native spherical harmonic transforms.

Scaffolding only. No transforms are implemented yet; see ``CLAUDE.md`` and
``ROADMAP.md`` at the repository root. The first development action is the
Phase-0 feasibility spike (spin-0 + spin-2 HEALPix forward/inverse validated
against ducc0 / healpy, with hard accuracy gates).
"""

__version__ = "0.0.0"
